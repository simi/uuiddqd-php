// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("uuid", "for uuid support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("uuid", "enable uuid support", "no");

if (PHP_UUID != "no") {
	EXTENSION("uuid", "uuid.c");
}

